{
	"name": "skeleton",
	"version": "1.0.0",
	"description": "",
	"private": true,
	"scripts": {
		"preinstall": "npx only-allow pnpm",
		"test": "echo \"Error: no test specified\" && exit 1",
		"getall": "node scripts/checkout.cjs",
		"postinstall": "pnpm -r gen:alias",
		"site": "create-skeleton-app --types=typescript --prettier --eslint --playwright=false --vitest=false --codeblocks=true --popups=true --typography=false --forms=false -t=skeleton --skeletontemplate=welcome -p=sites --monorepo"
	},
	"license": "MIT",
	"homepage": "https://skeleton.dev/",
	"repository": {
		"type": "git",
		"url": "https://github.com/skeletonlabs/skeleton.git"
	},
	"bugs": {
		"url": "https://github.com/skeletonlabs/skeleton/issues"
	},
	"devDependencies": {
		"create-skeleton-app": "workspace:^",
		"cspell": "^6.31.1",
		"shelljs": "^0.8.5"
	},
	"packageManager": "pnpm@7.30.3"
}
