import { expect, test } from 'vitest';

test('it works', () => {
	expect(1 + 1).toBe(2);
});

// TODO: Write proper test suite, possibly mocking the File System to assert on before/after migration files.
