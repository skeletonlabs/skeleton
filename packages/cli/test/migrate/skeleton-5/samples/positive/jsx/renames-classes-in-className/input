export function Field({ active }) {
	return (
		<div className="input-group grid-cols-[auto_1fr_auto]">
			<input className="ig-input" />
			<select className="ig-select" />
			<button className={clsx('ig-btn', active && 'base-font-family')}>Go</button>
			<p className="heading-font-color-dark">hi</p>
			<a className="card card-hover" href="/">Card</a>
		</div>
	);
}
