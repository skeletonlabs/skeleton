---
layout: '@lib/layouts/LayoutDoc.astro'
title: Accordions
description: Divide content into collapsible sections.
srcSvelte: '/components/accordion'
srcReact: '/components/accordion'
srcAlly: 'https://www.w3.org/WAI/ARIA/apg/patterns/accordion/'
showDocsUrl: true
# ---
tableProps: [
    { label: 'Accordion', data: [
        { name: 'autocollapse', type: 'boolean', value: 'false', description: 'Set the auto-collapse mode.' }
    ]},
    { label: 'AccordionItem', data: [
        { name: 'open', type: 'boolean', value: 'false', description: 'Set open by default on load.' }
    ]},
]
tableChildren: [
    { label: 'Accordion', data: [
        { name: 'default', description: 'Accepts component children.' }
    ]},
]
tableEvents: [
    { label: 'Accordion', data: [
        { name: 'onToggle', element: 'button', description: 'Fires when an accordion item is toggled.' }
    ]},
]
tableA11y: [
    { keys: [Tab], description: 'Focus the next accordion item.' }
]
---

import componentSet from "@lib/components/mdx/index";
export const components = componentSet;

import { Code } from 'astro-expressive-code/components';

import { Preview } from '@lib/components/react/Preview.tsx'
import { Framework } from '@lib/components/react/Framework.tsx'
import Table from "@lib/components/astro/Table.astro";

import { Page as ExReact } from "@lib/previews/components/accordions/Example.tsx";
import CodeReact from "@lib/previews/components/accordions/Example.tsx?raw";
import ExSvelte from "@lib/previews/components/accordions/Example.svelte";
import CodeSvelte from "@lib/previews/components/accordions/Example.svelte?raw";

{/* --- */}

## Preview

### React

<Preview client:load>
    <Fragment slot="preview"><ExReact client:load /></Fragment>
    <Fragment slot="code"><Code code={CodeReact} lang="ts" /></Fragment>
</Preview>

### Svelte

<Preview client:load>
    <Fragment slot="preview"><ExSvelte client:load /></Fragment>
    <Fragment slot="code"><Code code={CodeSvelte} lang="ts" /></Fragment>
</Preview>

## Usage

The {frontmatter.title} usage goes here.

<Framework client:load>
    <Fragment slot="react">
        React - Lorem ipsum dolor sit amet consectetur adipisicing elit. Sunt itaque accusantium minus earum aliquam doloremque nostrum modi quaerat tempore maxime, nobis possimus in facere voluptates saepe autem. Ab, ea iusto.
    </Fragment>
    <Fragment slot="svelte">
        Svelte - Lorem ipsum dolor sit amet consectetur adipisicing elit. Sunt itaque accusantium minus earum aliquam doloremque nostrum modi quaerat tempore maxime, nobis possimus in facere voluptates saepe autem. Ab, ea iusto.
    </Fragment>
</Framework>

{/* WARNING: the following is a test, do not replicate on other doc pages. */}

### Props

{frontmatter.tableProps.map(table => (
    <Table label={table.label} data={table.data} tonal />
))}

### Children

{frontmatter.tableChildren.map(table => (
    <Table label={table.label} data={table.data} tonal />
))}

### Events

{frontmatter.tableEvents.map(table => (
    <Table label={table.label} data={table.data} tonal />
))}

### Accessibility

<Table data={frontmatter.tableA11y} kbd />