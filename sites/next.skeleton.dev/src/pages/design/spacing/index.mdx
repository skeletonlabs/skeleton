---
layout: '@lib/layouts/LayoutDoc.astro'
title: Spacing
description: The Skeleton spacing system.
srcCore: '/plugin/extends/spacing.ts'
showDocsUrl: true
---

export const components = componentSet

import Spacing from "./Spacing.astro";

## Supports

`padding`, `margin`, `width`, `minWidth`, `maxWidth`, `height`, `minHeight`, `maxHeight`, `gap`, `inset`, `space`, `translate`.

This extends the [Tailwind spacing system](https://tailwindcss.com/docs/customizing-spacing) to enable dynamic scaling.

<Spacing />