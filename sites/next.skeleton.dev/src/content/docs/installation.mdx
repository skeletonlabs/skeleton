---
layout: '@layouts/LayoutDoc.astro'
title: Installation
description: Learn how to get started with Skeleton.
orderBy: 1
---

export const components = componentSet;

import Process from '@components/docs/Process.astro';
import ProcessStep from '@components/docs/ProcessStep.astro';

<Process>
    <ProcessStep step="1">
        ## Create a Project
        Start by creating a new SvelteKit project using `create-svelte-app`:
        ```console
        npm create svelte@latest my-skeleton-app
        cd my-skeleton-app
        npm install
        npm run dev -- --open
        ```
    </ProcessStep>
    <ProcessStep step="2">
        ## Install Skeleton
        Install the Skeleton core and Skeleton Svelte packages.
        ```console
        npm i -D @skeletonlabs/skeleton @skeletonlabs/skeleton-svelte
        ```
    </ProcessStep>
    <ProcessStep step="3">
        ## Install Tailwind
        Install Tailwind using [Svelte-Add](https://github.com/svelte-add/tailwindcss) to automate the process.
        ```console
        npx svelte-add@latest tailwindcss
        npm install
        ```
    </ProcessStep>
    <ProcessStep step="4">
        ## Configure the Tailwind Plugin
        Open `tailwind.config.js`, then implement the highlighted snippets below.
```js {2, 3, 9, 15-17}
import { join } from 'path';
import { skeleton } from '@skeletonlabs/skeleton';
import * as themes from "@skeletonlabs/skeleton/themes";

/** @type {import('tailwindcss').Config} */
export default {
    content: [
        './src/**/*.{html,js,svelte,ts}',
        join(require.resolve('@skeletonlabs/skeleton-svelte'), '../**/*.{html,js,svelte,ts}')
    ],
    theme: {
        extend: {},
    },
    plugins: [
        skeleton({
            themes: [ theme.cerberus, theme.rose ]
        })
    ]
}
```
    </ProcessStep>
    <ProcessStep step="5">
        ## Set your Active Theme
        Open `app.html`, then set the `data-theme` attribute to define the active theme.
```html
<body data-theme="cerberus">...</body>
```
    </ProcessStep>
    <ProcessStep step="check">
        ## Ready
        You're now ready to begin using Skeleton. To verify everything is working, try [adding a button](/tailwind/buttons) to your project.
    </ProcessStep>
</Process>