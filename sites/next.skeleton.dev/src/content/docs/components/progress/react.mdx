---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Example from '@examples/components/progress/Example.tsx';
import ExampleRaw from '@examples/components/progress/Example.tsx?raw';

import ExampleIndeterminate from '@examples/components/progress/ExampleIndeterminate.tsx';
import ExampleIndeterminateRaw from '@examples/components/progress/ExampleIndeterminate.tsx?raw';

{/* --- */}

<Preview client:load>
	<Fragment slot="preview">
		<Example client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="tsx" />
	</Fragment>
</Preview>

## Indeterminate

When the `value` prop is left out or is passed `undefined` the progress will be marked as underminate and show a loading animation.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleIndeterminate client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleIndeterminateRaw} lang="tsx" />
	</Fragment>
</Preview>
