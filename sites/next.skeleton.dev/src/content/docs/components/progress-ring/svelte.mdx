---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Example from '@examples/components/progress-rings/Example.svelte';
import ExampleRaw from '@examples/components/progress-rings/Example.svelte?raw';
import ExampleIcons from '@examples/components/progress-rings/ExampleIcons.svelte';
import ExampleIconsRaw from '@examples/components/progress-rings/ExampleIcons.svelte?raw';
import ExampleIndeterminate from '@examples/components/progress-rings/ExampleIndeterminate.svelte';
import ExampleIndeterminateRaw from '@examples/components/progress-rings/ExampleIndeterminate.svelte?raw';

{/* --- */}

<Preview client:load>
	<Fragment slot="preview">
		<Example client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

## Icons

Provide icons or other content using the `children` snippet.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleIcons client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleIconsRaw} lang="svelte" />
	</Fragment>
</Preview>

## Indeterminate

Uses indeterminate mode when `value` is explicitly set to `null`.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleIndeterminate client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleIndeterminateRaw} lang="svelte" />
	</Fragment>
</Preview>
