---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Example from '@examples/components/ratings/Example.svelte';
import ExampleRaw from '@examples/components/ratings/Example.svelte?raw';
import CustomIconsExample from '@examples/components/ratings/CustomIconsExample.svelte';
import CustomIconsExampleRaw from '@examples/components/ratings/CustomIconsExample.svelte?raw';
import UserInteractiveExample from '@examples/components/ratings/UserInteractiveExample.svelte';
import UserInteractiveExampleRaw from '@examples/components/ratings/UserInteractiveExample.svelte?raw';
import FractionExample from '@examples/components/ratings/FractionExample.svelte';
import FractionExampleRaw from '@examples/components/ratings/FractionExample.svelte?raw';
import RTLExample from '@examples/components/ratings/RTLExample.svelte';
import RTLExampleRaw from '@examples/components/ratings/RTLExample.svelte?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Example client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

Perfect for creating a stars ratings for movies or ecommerce ratings. Supply any type of image, text, or icon for the `iconEmpty` and `iconFull` slots. Then adjust the `value` and `max` properties as desired. Note that SVG icons displayed verbatim, so you may need to adjust the fill color and responsive sizing.

## Using Icons

<Preview client:load>
	<Fragment slot="preview">
		<CustomIconsExample client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={CustomIconsExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

## Interactive

<Preview client:load>
	<Fragment slot="preview">
		<UserInteractiveExample client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={UserInteractiveExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

## Fractions

Use the Prop `fraction` to set how many fractions a rating has.

<Preview client:load>
	<Fragment slot="preview">
		<FractionExample client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={FractionExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

## RTL

<Preview client:load>
	<Fragment slot="preview">
		<RTLExample client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={RTLExampleRaw} lang="svelte" />
	</Fragment>
</Preview>