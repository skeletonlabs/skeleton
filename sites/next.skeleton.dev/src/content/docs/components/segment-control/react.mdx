---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import { Page as Example } from '@examples/components/segment-control/Example.tsx';
import ExampleRaw from '@examples/components/segment-control/Example.tsx?raw';
import { Page as ExampleDisabled } from '@examples/components/segment-control/ExampleDisabled.tsx';
import ExampleDisabledRaw from '@examples/components/segment-control/ExampleDisabled.tsx?raw';
import { Page as ExampleVertical } from '@examples/components/segment-control/ExampleVertical.tsx';
import ExampleVerticalRaw from '@examples/components/segment-control/ExampleVertical.tsx?raw';

{/* --- */}

<Preview client:load>
	<Fragment slot="preview">
		<Example client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="tsx" />
	</Fragment>
</Preview>

## Disabled

Set `disabled` to any item to disable focus and interaction.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleDisabled client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleDisabledRaw} lang="tsx" />
	</Fragment>
</Preview>

## Vertical

Set `flexDirection` to enable a vertical layout. This can controlled with responsive breakpoints.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleVertical client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleVerticalRaw} lang="tsx" />
	</Fragment>
</Preview>

## Form Control

This component wraps native radio inputs, which enables it to operate as as a form control.

## Alternative

Consider using a [Button Group](/docs/tailwind/buttons#group) if you need direct control over the markup and styling.
