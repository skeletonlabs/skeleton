---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Example from '@examples/components/segment-control/Example.svelte';
import ExampleRaw from '@examples/components/segment-control/Example.svelte?raw';
import ExampleDisabled from '@examples/components/segment-control/ExampleDisabled.svelte';
import ExampleDisabledRaw from '@examples/components/segment-control/ExampleDisabled.svelte?raw';
import ExampleVertical from '@examples/components/segment-control/ExampleVertical.svelte';
import ExampleVerticalRaw from '@examples/components/segment-control/ExampleVertical.svelte?raw';

{/* --- */}

<Preview client:load>
	<Fragment slot="preview">
		<Example client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

## Disabled

Set `disabled` to any item to disable focus and interaction.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleDisabled client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleDisabledRaw} lang="svelte" />
	</Fragment>
</Preview>

## Vertical

Set `flexDirection` to enable a vertical layout. This can be controlled with responsive breakpoints.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleVertical client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleVerticalRaw} lang="svelte" />
	</Fragment>
</Preview>

## Form Control

This component wraps native radio inputs, which enables it to operate as as a form control.

## Alternative

Consider using a [Button Group](/docs/tailwind/buttons#group) if you need direct control over the markup and styling.
