---
layout: '@layouts/LayoutDoc.astro'
title: SVG Filters
description: Apply filter effects to elements and images.
showDocsUrl: true
pubDate: 2024-11-19
tags: ['native', 'effects']
---

export const components = componentSet;

import Example from '@examples/resources/cookbook/svg-filters/Example.astro';
import ExampleRaw from '@examples/resources/cookbook/svg-filters/Example.astro?raw';

## How It Works

This feature is enabled by [SVG filters](https://developer.mozilla.org/en-US/docs/Web/SVG/Element/filter) paired with [feColorMatrix](https://developer.mozilla.org/en-US/docs/Web/SVG/Element/feColorMatrix) transformations.

## Minimal Example


The SVG controls the filter settings. The filter is applied using `filter: url(#Emerald)` and linking the SVG's unique ID.

<Preview client:visible>
	<Fragment slot="preview">
		<Example />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="astro" />
	</Fragment>
</Preview>

## Create a Filter

We recommend [SVG Color Matrix Mixer](https://fecolormatrix.com/) by [Rik Schennink](https://x.com/rikschennink/) to create your own filters.

## From Skeleton

Skeleton provides a collection of [open source SVG filters](https://github.com/skeletonlabs/svg-filters). Pull requests welcome!

<img src="https://i.imgur.com/lI7sUfs.png" class="w-full border border-surface-200-800 rounded-container overflow-hidden shadow" alt="Previews" />

## Tips

- The SVG must be in the same scope as the elements being filtering.
- Consder storing your SVGs witin your local project for quick and reusable imports.
- All Vite-based frameworks support [.svg imports](https://vite.dev/guide/assets.html#importing-asset-as-url).
- Optionally you can embed the SVG within a component (ex: `.svelte`, `.tsx`), then import that.
- Filter SVGs are affected by the flow DOM, including class styles such as `space-{x|y}`.
