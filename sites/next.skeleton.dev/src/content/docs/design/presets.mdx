---
layout: '@layouts/LayoutDoc.astro'
title: Presets
description: Utility classes that mix colors and contrast values.
srcCore: '/plugin/utilities/presets.ts'
order: 20
---

export const components = componentSet;

import Example from '@examples/design/presets/Example.svelte';
import ExampleRaw from '@examples/design/presets/Example.svelte?raw';
import ExampleFilled from '@examples/design/presets/ExampleFilled.astro';
import ExampleFilledRaw from '@examples/design/presets/ExampleFilled.astro?raw';
import ExampleTonal from '@examples/design/presets/ExampleTonal.astro';
import ExampleTonalRaw from '@examples/design/presets/ExampleTonal.astro?raw';
import ExampleOutlined from '@examples/design/presets/ExampleOutlined.astro';
import ExampleOutlinedRaw from '@examples/design/presets/ExampleOutlined.astro?raw';
import ExampleCustom from '@examples/design/presets/ExampleCustom.astro';
import ExampleCustomRaw from '@examples/design/presets/ExampleCustom.astro?raw';
import ExampleGradient from '@examples/design/presets/ExampleGradient.astro';
import ExampleGradientRaw from '@examples/design/presets/ExampleGradient.astro?raw';

Presets are complex utilty classes that combine various Skeleton primitives, allowing you a quick way to style buttons, badges, cards, and more. Create any combination of styles by mixing Skeleton Presets and Tailwind primatives.

<Preview client:load>
	<Fragment slot="preview">
		<Example client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

1. **Filled** - a filled preset of the primary brand color.
2. **Tonal** - a tonal preset of the primary brand color.
3. **Outlined** - an outlined preset of the primary brand color.
4. **Elevated** - mixes a filled preset with a shadow.
5. **Ghost** - has no style by default, but shows a tonal preset on hover.
6. **Ghost Icon** - has no style by default, but shows a branded tonal preset on hover.
7. **Gradient** - a custom preset generated using Tailwind gradient primitives.

## Options

### Filled

```
preset-filled-{color}-{shade}-{shade}
```

<Preview client:load>
	<Fragment slot="preview">
		<ExampleFilled />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleFilledRaw} lang="astro" />
	</Fragment>
</Preview>

### Tonal

```
preset-tonal-{color}
```

<Preview client:load>
	<Fragment slot="preview">
		<ExampleTonal />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleTonalRaw} lang="astro" />
	</Fragment>
</Preview>

### Outlined

```
preset-outlined-{color}-{shade}-{shade}
```

<Preview client:load>
	<Fragment slot="preview">
		<ExampleOutlined />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleOutlinedRaw} lang="astro" />
	</Fragment>
</Preview>

## Customization

### Custom Presets

Since Presets are just a combination of Skeleton and Tailwind primitives, you can abstract these to your global stylesheet to create your own custom preset styles. We recommend the `preset-{foo}` naming scheme to keep this standardized.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleCustom client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleCustomRaw} lang="astro" />
	</Fragment>
</Preview>

### Gradient Presets

Tailwind provides a number of powerful [Gradient utilities](https://tailwindcss.com/docs/gradient-color-stops). Use these to create any combination of gradient presets.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleGradient client:load />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleGradientRaw} lang="svelte" />
	</Fragment>
</Preview>
