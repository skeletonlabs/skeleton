---
layout: '@lib/layouts/LayoutDoc.astro'
title: Colors
description: The Skeleton color system.
srcCore: '/plugin/extends/colors.ts'
showDocsUrl: true
---

export const components = componentSet

import Swatches from '@lib/previews/design/colors/Swatches.astro';
import Pairings from '@lib/previews/design/colors/Pairings.astro';

## Palette

```
.bg-{color}-{shade}
```

<Swatches />

## Pairings

Supports: `accent`, `bg`, `border`, `caret`, `decoration`, `divide`, `outline`, `ring`, `shadow`, `stroke` `text`.

```
.bg-{color}-{l}-{d}
```

<Pairings />
