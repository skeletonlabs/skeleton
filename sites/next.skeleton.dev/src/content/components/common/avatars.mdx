---
title: Avatars
description: Display user avatars with an image or initials.
srcSvelte: '/src/lib/components/avatar'
srcReact: '/src/components/avatar'
showDocsUrl: true
---

export const components = componentSet;

## Fallback

The fallback __children__, suitable for Initials, Icons, and fallback images, are exclusively shown when the `imageSrc` prop is empty.

```tsx
<Avatar>SK</Avatar>
<Avatar>💀</Avatar>
```