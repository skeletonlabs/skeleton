---
layout: '@layouts/LayoutDoc.astro'
title: Tables
description: Provides a set of styles for native HTML table elements.
srcCore: '/plugin/components/tables.css'
showDocsUrl: true
---

export const components = componentSet

import Example from "@examples/tailwind/tables/Example.astro";
import ExampleRaw from "@examples/tailwind/tables/Example.astro?raw";

<Preview client:load>
    <Fragment slot="preview"><Example client:load /></Fragment>
    <Fragment slot="code">
        We've used Svelte here, but the concept should work for any framework.
        <Code code={ExampleRaw} lang="ts" class="mt-4" />
    </Fragment>
</Preview>

## Layout

See [Tailwind's utility classes](https://tailwindcss.com/docs/table-layou) for adjusting the table layout algorithm. Apply this to the Table element.