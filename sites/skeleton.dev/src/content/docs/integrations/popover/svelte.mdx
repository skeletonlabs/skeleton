---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import PositioningOptions from '@examples/integrations/popover/PositioningOptions.txt?raw';
import ExamplePopover from '@examples/integrations/popover/ExamplePopover.svelte';
import ExamplePopoverRaw from '@examples/integrations/popover/ExamplePopover.svelte?raw';
import ExampleTooltip from '@examples/integrations/popover/ExampleTooltip.svelte';
import ExampleTooltipRaw from '@examples/integrations/popover/ExampleTooltip.svelte?raw';
import ExampleCombobox from '@examples/integrations/popover/ExampleCombobox.svelte';
import ExampleComboboxRaw from '@examples/integrations/popover/ExampleCombobox.svelte?raw';
import ExampleModal from '@examples/integrations/popover/ExampleModal.svelte';
import ExampleModalRaw from '@examples/integrations/popover/ExampleModal.svelte?raw';
import ExampleModalDrawer from '@examples/integrations/popover/ExampleModalDrawer.svelte';
import ExampleModalDrawerRaw from '@examples/integrations/popover/ExampleModalDrawer.svelte?raw';

import schemaPopover from '@content/schemas/svelte/popover.json';
import schemaTooltip from '@content/schemas/svelte/tooltip.json';
import schemaCombobox from '@content/schemas/svelte/combobox.json';
import schemaModal from '@content/schemas/svelte/modal.json';

## Warning

<div class="card preset-tonal-warning preset-outlined-warning-500 p-8 space-y-8">
	The following components have been introduced as a temporary solution until either Floating UI provides Svelte bindings, the upcoming
	native browser APIs become widely available, or another alternative is introduced (whichever comes first). Until that time, Skeleton will
	continue to maintain these components for production use. Maintenance will continue until the next major version of Skeleton if an
	alternative is introduced.
</div>

The following popover components are built using [Zag.js](https://zagjs.com/) primitives, and utilize [Floating UI](https://floating-ui.com/) to control positioning and anchoring of each related element.

---

## Z-Index

Neither Skeleton, Zag, or Floating UI take an opinionated stance regarding z-index stacking. This means elements may be occluded beneath other elements with a higher index. To adjust this value, set `zIndex` with a raw CSS value, such as `10`, `20`, `999` - provided as a string. Note that this property <u>does not</u> accept utility classes.

```svelte
<Popover ... zIndex="10">
	<!-- ... -->
</Popover>
```

- `zIndex="z-50"` - This will NOT work.
- `zIndex={99}` - This will NOT work.
- `zIndex="99"` - This will work as expected.

## Arrows

Both `<Popover>` and `<Tooltip>` allow for arrows. Skeleton follows [Zag's conventions](https://zagjs.com/components/svelte/tooltip#styling-the-arrow) for styling arrows. Add the `arrow` prop to enable the arrow, then use the following to adjust each style. Note these properties <u>do not</u> accept utility classes.

| Prop Name         | CSS Custom Property  | Demo                                                      |
| ----------------- | -------------------- | --------------------------------------------------------- |
| `arrow`           | -                    | `<Tooltip arrow>`                                         |
| `arrowBackground` | `--arrow-background` | `<Tooltip arrowBackground="var(--color-surface-50-950)">` |
| `arrowSize`       | `--arrow-size`       | `<Tooltip arrowSize="10px">`                              |

{/* prettier-ignore */}
```svelte
<Tooltip
	...
	arrow
	arrowBackground="var(--color-surface-50-950)"
	arrowSize="15px"
>
	...
</Tooltip>
```

---

## Popover

Triggers an anchored popover when you <u>click</u> the trigger element. [View API Reference](https://zagjs.com/components/react/popover#methods-and-properties).

<Preview client:load>
	<Fragment slot="preview">
		<ExamplePopover client:visible />
	</Fragment>
	<Fragment slot="code">
		<div class="space-y-4">
			<Code code={ExamplePopoverRaw} lang="svelte" />
			<Code code={PositioningOptions} lang="ts" />
		</div>
	</Fragment>
</Preview>

### Anatomy

<Anatomy label="Popover" element="span" isComponent>
	<Anatomy label="trigger" element="button" />
	<Anatomy label="positioner">
		<Anatomy label="arrow" />
		<Anatomy label="content" />
	</Anatomy>
</Anatomy>

### API Reference

<ApiTable schema={schemaPopover} />

## Tooltip

Triggers an anchored popover when you <u>hover</u> the trigger element. [View API Reference](https://zagjs.com/components/react/tooltip#methods-and-properties).

<Preview client:load>
	<Fragment slot="preview">
		<ExampleTooltip client:visible />
	</Fragment>
	<Fragment slot="code">
		<div class="space-y-4">
			<Code code={ExampleTooltipRaw} lang="svelte" />
			<Code code={PositioningOptions} lang="ts" />
		</div>
	</Fragment>
</Preview>

### Anatomy

<Anatomy label="Tooltip" element="span" isComponent>
	<Anatomy label="trigger" element="button" />
	<Anatomy label="positioner">
		<Anatomy label="arrow" />
		<Anatomy label="content" />
	</Anatomy>
</Anatomy>

### API Reference

<ApiTable schema={schemaTooltip} />

## Combobox

Triggers an anchored popover list when you tap the arrow. Includes auto-suggestion via typeahead. [View API Reference](https://zagjs.com/components/react/combobox#methods-and-properties).

<Preview client:load>
	<Fragment slot="preview">
		<ExampleCombobox client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleComboboxRaw} lang="svelte" />
	</Fragment>
</Preview>

Supplying a custom filter:

```ts
let selectedValue = $state(['']);
function customSearchFn(e) { ... }
```

{/* prettier-ignore */}
```svelte
<Combobox
	onValueChange={(e) => (selectedValue = e.value)}
	onInputValueChange={(e) => customSearchFn(e.inputValue)}
>...</Combobox>
```

### Anatomy

<Anatomy label="Combobox" element="span" isComponent>
	<Anatomy label="label">
		<Anatomy label="labelText" element="span" />
		<Anatomy label="inputGroup">
			<Anatomy label="inputGroupInput" element="input" />
			<Anatomy label="inputGroupButton" element="button">
				<Anatomy label="inputGroupArrow" element="svg" />
			</Anatomy>
		</Anatomy>
	</Anatomy>
	<Anatomy label="positioner">
		<Anatomy label="content" element="nav">
			<Anatomy label="option" element="button" />
		</Anatomy>
	</Anatomy>
</Anatomy>

### API Reference

<ApiTable schema={schemaCombobox} />

## Modal

Generate modals or dialogs that require a user's immediate attention. [View API Reference](https://zagjs.com/components/react/dialog#methods-and-properties).

<Preview client:load>
	<Fragment slot="preview">
		<ExampleModal client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleModalRaw} lang="svelte" />
	</Fragment>
</Preview>

<Preview client:load>
	<Fragment slot="preview">
		<ExampleModalDrawer client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleModalDrawerRaw} lang="svelte" />
	</Fragment>
</Preview>

### Anatomy

<Anatomy label="Modal" element="span" isComponent>
	<Anatomy label="trigger" element="button" />
	<Anatomy label="backdrop" />
	<Anatomy label="positioner">
		<Anatomy label="content" />
	</Anatomy>
</Anatomy>

### API Reference

<ApiTable schema={schemaModal} />

## Alternatives

### Svelte Attachment

For power users, we realize the above components may not cover all use cases. As such, we've provided a baseline Svelte attachment for a more direct integration with the Floating UI DOM library. [View our guide here](/docs/guides/cookbook/floating-ui-attachments).

### Native Browser APIs

Skeleton will always favor native browser APIs over third-party libraries such as Floating UI Svelte. The following is a list of current and upcoming incoming APIs we will aim to support in the future, but are not quite standardized cross-browser yet.

- [Dialog Cookbook](/docs/guides/cookbook/dialog)
- [Popover API](https://developer.mozilla.org/en-US/docs/Web/API/Popover_API)
- [CSS Anchoring Position](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_anchor_positioning)
