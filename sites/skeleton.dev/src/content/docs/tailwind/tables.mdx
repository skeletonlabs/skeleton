---
layout: '@/components/layouts/docs.astro'
title: Tables
description: Provides a set of styles for native HTML table elements.
srcCore: '/utilities/tables.css'
showDocsUrl: true
---

import Default from '@/components/examples/tailwind/tables/default.astro';
import DefaultRaw from '@/components/examples/tailwind/tables/default.astro?raw';
import Full from '@/components/examples/tailwind/tables/full.astro';
import FullRaw from '@/components/examples/tailwind/tables/full.astro?raw';
import Nav from '@/components/examples/tailwind/tables/nav.astro';
import NavRaw from '@/components/examples/tailwind/tables/nav.astro?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Default />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="astro" />
	</Fragment>
</Preview>

## Extras

Optionally add a header, footer, and caption.

<Preview client:load>
	<Fragment slot="preview">
		<Full />
	</Fragment>
	<Fragment slot="code">
		<Code code={FullRaw} lang="astro" />
	</Fragment>
</Preview>

## Navigation

Native HTML tables do not support interaction. For accessibility, use anchors or buttons within the last cell.

<Preview client:load>
	<Fragment slot="preview">
		<Nav />
	</Fragment>
	<Fragment slot="code">
		<Code code={NavRaw} lang="astro" />
	</Fragment>
</Preview>

## Layout

See [Tailwind's utility classes](https://tailwindcss.com/docs/table-layout) for adjusting the table layout algorithm. Apply this to the Table element.

## Hover Rows

Add a visual hover effect using the following Tailwind syntax.

```html
<tbody class="[&>tr]:hover:preset-tonal-primary">
	...
</tbody>
```

## Pagination

Pair with the Skeleton [Pagination](/docs/components/pagination/react) component for large data sets.
