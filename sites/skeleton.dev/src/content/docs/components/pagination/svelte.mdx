---
layout: '@layouts/LayoutDoc.astro'
---

import Default from '@examples/components/pagination/svelte/default.svelte';
import DefaultRaw from '@examples/components/pagination/svelte/default.svelte?raw';

import DynamicPageSize from '@examples/components/pagination/svelte/dynamic-page-size.svelte';
import DynamicPageSizeRaw from '@examples/components/pagination/svelte/dynamic-page-size.svelte?raw';

import Dir from '@examples/components/pagination/svelte/dir.svelte';
import DirRaw from '@examples/components/pagination/svelte/dir.svelte?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Default client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="svelte" />
	</Fragment>
</Preview>

## Page Size

<Preview client:load>
	<Fragment slot="preview">
		<DynamicPageSize client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DynamicPageSizeRaw} lang="svelte" />
	</Fragment>
</Preview>

## Direction

<Preview client:load>
	<Fragment slot="preview">
		<Dir client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DirRaw} lang="svelte" />
	</Fragment>
</Preview>

## Total Count

For server-side pagination, your data source may be truncated. Make sure to specify the total records using `count`.

```json
{
	"data": [...],
	"pagination": {
		"page": 1,
		"limit": 10,
		"count": 500,
	}
}
```

{/* prettier-ignore */}
```svelte
<Pagination
	page={response.pagination.page}
	count={response.pagination.count}
	pageSize={response.pagination.limit}
>
	...
</Pagination>
```

## API Reference

<ApiTable />
