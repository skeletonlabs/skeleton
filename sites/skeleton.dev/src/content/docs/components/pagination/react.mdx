---
layout: '@/layouts/LayoutDoc.astro'
---

import Default from '@/examples/components/pagination/react/default';
import DefaultRaw from '@/examples/components/pagination/react/default?raw';

import DynamicPageSize from '@/examples/components/pagination/react/dynamic-page-size';
import DynamicPageSizeRaw from '@/examples/components/pagination/react/dynamic-page-size?raw';

import Dir from '@/examples/components/pagination/react/dir';
import DirRaw from '@/examples/components/pagination/react/dir?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Default client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="tsx" />
	</Fragment>
</Preview>

## Page Size

<Preview client:load>
	<Fragment slot="preview">
		<DynamicPageSize client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DynamicPageSizeRaw} lang="tsx" />
	</Fragment>
</Preview>

## Direction

<Preview client:load>
	<Fragment slot="preview">
		<Dir client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DirRaw} lang="tsx" />
	</Fragment>
</Preview>

## Total Count

For server-side pagination, your data source may be truncated. Make sure to specify the total records using `count`.

```json
{
	"data": [...],
	"pagination": {
		"page": 1,
		"limit": 10,
		"count": 500,
	}
}
```

{/* prettier-ignore */}
```tsx
<Pagination
	page={response.pagination.page}
	count={response.pagination.count}
	pageSize={response.pagination.limit}
>
	...
</Pagination>
```

## API Reference

<ApiTable />
