---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Example from '@examples/components/toast/Example.svelte';
import ToasterReuseUtil from '@examples/components/toast/toaster-svelte.ts?raw';
import ExampleReuseLayoutRaw from '@examples/components/toast/ExampleReuseLayout.svelte?raw';
import ExampleReusePageRaw from '@examples/components/toast/ExampleReusePage.svelte?raw';

import ExampleType from '@examples/components/toast/ExampleType.svelte';
import ExampleTypeRaw from '@examples/components/toast/ExampleType.svelte?raw';
import ExamplePlacement from '@examples/components/toast/ExamplePlacement.svelte';
import ExamplePlacementRaw from '@examples/components/toast/ExamplePlacement.svelte?raw';
import ExampleClosable from '@examples/components/toast/ExampleClosable.svelte';
import ExampleClosableRaw from '@examples/components/toast/ExampleClosable.svelte?raw';
import ExamplePromise from '@examples/components/toast/ExamplePromise.svelte';
import ExamplePromiseRaw from '@examples/components/toast/ExamplePromise.svelte?raw';

The first example showcases the complete setup process for a reusable toast component, while the following are abridged to handle everything in a single file. Please keep this in mind when following these examples.

<Preview client:load>
	<Fragment slot="preview">
		<Example client:visible />
	</Fragment>
	<Fragment slot="code">
		<div class="space-y-4">
			Import `createToaster()` and export a new shared reference.
			<Code code={ToasterReuseUtil} lang="ts" title="toaster-svelte.ts" />
			Import the `<Toaster>` component and `toaster` reference in your root layout.
			<Code code={ExampleReuseLayoutRaw} lang="svelte" title="+layout.svelte" />
			Import `toaster` reference and use it to trigger toasts anywhere in your client-side app.
			<Code code={ExampleReusePageRaw} lang="svelte" title="+page.svelte" />
		</div>
	</Fragment>
</Preview>

## Trigger Methods

Use the generic `create()` method to trigger toasts of type: `info|success|warning|error`.

```ts
toaster.create({
	type: 'info',
	title: 'This is a toast',
	description: 'This is a toast description.'
});
```

Additionally, four shorthand methods are available for specific toast types.

- `info()`
- `success()`
- `warning()`
- `error()`

<Preview client:load>
	<Fragment slot="preview">
		<ExampleType client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleTypeRaw} lang="svelte" />
	</Fragment>
</Preview>

## Placement

Provide a `placement` option for `createToaster()`. Accepts the following:

- `top-start`
- `top-end`
- `bottom-start`
- `bottom-end`

<Preview client:load>
	<Fragment slot="preview">
		<ExamplePlacement client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExamplePlacementRaw} lang="svelte" />
	</Fragment>
</Preview>

## Closable

Set `closable` to `false` to disable the close button toasts.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleClosable client:visible closable={false} />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleClosableRaw} lang="svelte" />
	</Fragment>
</Preview>

## Promise

Use `promise()` to create a toast configured based on the state of a promise. The toast will automatically update based on the state of the promise.

<Preview client:load>
	<Fragment slot="preview">
		<ExamplePromise client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExamplePromiseRaw} lang="svelte" />
	</Fragment>
</Preview>

## API Reference

<ApiTable />

## Alternatives

If you're looking for power user features or more control over templates, consider using [Svelte-French-Toast](https://svelte-french-toast.com/).

```

```
