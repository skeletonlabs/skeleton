---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Default from '@examples/components/toast/react/default';
import DefaultRaw from '@examples/components/toast/react/default?raw';

import Type from '@examples/components/toast/react/type';
import TypeRaw from '@examples/components/toast/react/type?raw';

import Action from '@examples/components/toast/react/action';
import ActionRaw from '@examples/components/toast/react/action?raw';

import Closable from '@examples/components/toast/react/closable';
import ClosableRaw from '@examples/components/toast/react/closable?raw';

import Placement from '@examples/components/toast/react/placement';
import PlacementRaw from '@examples/components/toast/react/placement?raw';

import Meta from '@examples/components/toast/react/meta';
import MetaRaw from '@examples/components/toast/react/meta?raw';

import Promise from '@examples/components/toast/react/promise';
import PromiseRaw from '@examples/components/toast/react/promise?raw';

The following examples all showcase a one off example of `<Toast.Group>`, in a real world scenario you would likely want to put a single `<Toast.Group>` at the root of your app and use a single `toaster` instance to manage all toasts.

<Preview client:load>
	<Fragment slot="preview">
		<Default client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="tsx" />
	</Fragment>
</Preview>

## Type

<Preview client:load>
	<Fragment slot="preview">
		<Type client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={TypeRaw} lang="tsx" />
	</Fragment>
</Preview>

## Action

<Preview client:load>
	<Fragment slot="preview">
		<Action client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ActionRaw} lang="tsx" />
	</Fragment>
</Preview>

## Closable

<Preview client:load>
	<Fragment slot="preview">
		<Closable client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ClosableRaw} lang="tsx" />
	</Fragment>
</Preview>

## Placement

<Preview client:load>
	<Fragment slot="preview">
		<Placement client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={PlacementRaw} lang="tsx" />
	</Fragment>
</Preview>

## Meta

<Preview client:load>
	<Fragment slot="preview">
		<Metadata client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={MetadataRaw} lang="tsx" />
	</Fragment>
</Preview>

## Promise

<Preview client:load>
	<Fragment slot="preview">
		<Promise client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={PromiseRaw} lang="tsx" />
	</Fragment>
</Preview>

## API Reference

<ApiTable />
