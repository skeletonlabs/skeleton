---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import { Page as Example } from '@examples/components/progress-rings/Example.tsx';
import ExampleRaw from '@examples/components/progress-rings/Example.tsx?raw';
import { Page as ExampleLabel } from '@examples/components/progress-rings/ExampleLabel.tsx';
import ExampleLabelRaw from '@examples/components/progress-rings/ExampleLabel.tsx?raw';
import { Page as ExampleIcons } from '@examples/components/progress-rings/ExampleIcons.tsx';
import ExampleIconsRaw from '@examples/components/progress-rings/ExampleIcons.tsx?raw';
import { Page as ExampleIndeterminate } from '@examples/components/progress-rings/ExampleIndeterminate.tsx';
import ExampleIndeterminateRaw from '@examples/components/progress-rings/ExampleIndeterminate.tsx?raw';

<Preview client:load>
	<Fragment slot="preview">
		{/* NOTE: set client:only to prevent page load bug */}
		<Example client:only="react" />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="tsx" />
	</Fragment>
</Preview>

## Label

Display an auto-scaled percentage label using the `showLabel` prop.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleLabel client:only="react" />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleLabelRaw} lang="tsx" />
	</Fragment>
</Preview>

## Icons

Provide icons or other content using the component `children`.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleIcons client:only="react" />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleIconsRaw} lang="tsx" />
	</Fragment>
</Preview>

## Indeterminate

Uses indeterminate mode when `value` is explicitly set to `null`.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleIndeterminate client:only="react" />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleIndeterminateRaw} lang="tsx" />
	</Fragment>
</Preview>

## Anatomy

<Anatomy label="ProgressRing" tag="figure" isComponent>
	<Anatomy label="children" />
	<Anatomy label="svg" tag="svg">
		<Anatomy label="track" tag="circle" />
		<Anatomy label="meter" tag="circle" />
		<Anatomy label="label" tag="text" />
	</Anatomy>
</Anatomy>

## API Reference

The Progress Ring component is an SVG-based element where all internal elements are drawn **relative** to the SVG's size.

For example, the `strokeWidth` property can accept a pixel (px) value, but this value is scaled proportionally to the size of the SVG rather than being an absolute pixel measurement.

<ApiTable />
