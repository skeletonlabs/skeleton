---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Default from '@examples/components/progress-linear/react/default';
import DefaultRaw from '@examples/components/progress-linear/react/default?raw';

import Color from '@examples/components/progress-linear/react/color';
import ColorRaw from '@examples/components/progress-linear/react/color?raw';

import Height from '@examples/components/progress-linear/react/height';
import HeightRaw from '@examples/components/progress-linear/react/height?raw';

import Orientation from '@examples/components/progress-linear/react/orientation';
import OrientationRaw from '@examples/components/progress-linear/react/orientation?raw';

import Indeterminate from '@examples/components/progress-linear/react/indeterminate';
import IndeterminateRaw from '@examples/components/progress-linear/react/indeterminate?raw';

import CustomAnimation from '@examples/components/progress-linear/react/custom-animation';
import CustomAnimationRaw from '@examples/components/progress-linear/react/custom-animation?raw';

import Native from '@examples/components/progress-linear/react/native';
import NativeRaw from '@examples/components/progress-linear/react/native?raw';

import Dir from '@examples/components/progress-linear/react/dir';
import DirRaw from '@examples/components/progress-linear/react/dir?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Default client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="tsx" />
	</Fragment>
</Preview>

## Color

Provide background color values for the track and range to customize the colors.

<Preview client:load>
	<Fragment slot="preview">
		<Color client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ColorRaw} lang="tsx" />
	</Fragment>
</Preview>

## Height

<Preview client:load>
	<Fragment slot="preview">
		<Height client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={HeightRaw} lang="tsx" />
	</Fragment>
</Preview>

## Orientation

For vertical orieintation, a height must be defined on the Track component. The default is `h-[100px]`.

<Preview client:load>
	<Fragment slot="preview">
		<Orientation client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={OrientationRaw} lang="tsx" />
	</Fragment>
</Preview>

## Indeterminate

You must explicitely specify a `null` value to trigger indeterminate mode.

<Preview client:load>
	<Fragment slot="preview">
		<Indeterminate client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={IndeterminateRaw} lang="tsx" />
	</Fragment>
</Preview>

### Custom Animation

<Preview client:load>
	<Fragment slot="preview">
		<CustomAnimation client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={CustomAnimationRaw} lang="tsx" />
	</Fragment>
</Preview>

Consider using following variants to target the orientation and state.

- `data-[orientation=horizontal]:my-custom-animation` - target the horizontal orientation.
- `data-[orientation=vertical]:my-custom-animation` - target the vertical orientation.
- `data-[state=indeterminate]:my-custom-animation` - target the indeterminate state.

## Native Alternative

The native [`progress`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/progress) element is available cross browser, but does not support indeterminate animations.

<Preview client:load>
	<Fragment slot="preview">
		<Native client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={NativeRaw} lang="tsx" />
	</Fragment>
</Preview>

## Direction

<Preview client:load>
	<Fragment slot="preview">
		<Dir client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DirRaw} lang="tsx" />
	</Fragment>
</Preview>

## API Reference

<ApiTable component="progress" />
