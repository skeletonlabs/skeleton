---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Example from '@examples/components/progress-linear/react/default.tsx';
import ExampleRaw from '@examples/components/progress-linear/react/default.tsx?raw';

import ExampleColor from '@examples/components/progress-linear/react/color';
import ExampleColorRaw from '@examples/components/progress-linear/react/color.tsx?raw';

import ExampleHeight from '@examples/components/progress-linear/react/height';
import ExampleHeightRaw from '@examples/components/progress-linear/react/height.tsx?raw';

import ExampleOrientation from '@examples/components/progress-linear/react/orientation';
import ExampleOrientationRaw from '@examples/components/progress-linear/react/orientation.tsx?raw';

import ExampleIndeterminate from '@examples/components/progress-linear/react/indeterminate';
import ExampleIndeterminateRaw from '@examples/components/progress-linear/react/indeterminate.tsx?raw';

import ExampleCustomAnimation from '@examples/components/progress-linear/react/custom-animation';
import ExampleCustomAnimationRaw from '@examples/components/progress-linear/react/custom-animation.tsx?raw';

import ExampleNative from '@examples/components/progress-linear/react/native';
import ExampleNativeRaw from '@examples/components/progress-linear/react/native.tsx?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Example client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="tsx" />
	</Fragment>
</Preview>

## Color

Provide background color values for the track and range to customize the colors.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleColor client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleColorRaw} lang="tsx" />
	</Fragment>
</Preview>

## Height

<Preview client:load>
	<Fragment slot="preview">
		<ExampleHeight client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleHeightRaw} lang="tsx" />
	</Fragment>
</Preview>

## Orientation

For vertical orieintation, a height must be defined on the Track component. The default is `h-[100px]`.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleOrientation client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleOrientationRaw} lang="tsx" />
	</Fragment>
</Preview>

## Indeterminate

You must explicitely specify a `null` value to trigger indeterminate mode.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleIndeterminate client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleIndeterminateRaw} lang="tsx" />
	</Fragment>
</Preview>

### Custom Animation

<Preview client:load>
	<Fragment slot="preview">
		<ExampleCustomAnimation client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleCustomAnimationRaw} lang="tsx" />
	</Fragment>
</Preview>

Consider using following variants to target the orientation and state.

- `data-[orientation=horizontal]:my-custom-animation` - target the horizontal orientation.
- `data-[orientation=vertical]:my-custom-animation` - target the vertical orientation.
- `data-[state=indeterminate]:my-custom-animation` - target the indeterminate state.

## Native Alternative

The native [`progress`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/progress) element is available cross browser, but does not support indeterminate animations.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleNative client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleNativeRaw} lang="tsx" />
	</Fragment>
</Preview>

## API Reference

<ApiTable component="progress" />
