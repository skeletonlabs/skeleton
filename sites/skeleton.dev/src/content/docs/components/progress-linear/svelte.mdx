---
layout: '@layouts/LayoutDoc.astro'
---

import Default from '@/examples/components/progress-linear/svelte/default.svelte';
import DefaultRaw from '@/examples/components/progress-linear/svelte/default.svelte?raw';

import Color from '@/examples/components/progress-linear/svelte/color.svelte';
import ColorRaw from '@/examples/components/progress-linear/svelte/color.svelte?raw';

import Height from '@/examples/components/progress-linear/svelte/height.svelte';
import HeightRaw from '@/examples/components/progress-linear/svelte/height.svelte?raw';

import Orientation from '@/examples/components/progress-linear/svelte/orientation.svelte';
import OrientationRaw from '@/examples/components/progress-linear/svelte/orientation.svelte?raw';

import Indeterminate from '@/examples/components/progress-linear/svelte/indeterminate.svelte';
import IndeterminateRaw from '@/examples/components/progress-linear/svelte/indeterminate.svelte?raw';

import CustomAnimation from '@/examples/components/progress-linear/svelte/custom-animation.svelte';
import CustomAnimationRaw from '@/examples/components/progress-linear/svelte/custom-animation.svelte?raw';

import Native from '@/examples/components/progress-linear/svelte/native.svelte';
import NativeRaw from '@/examples/components/progress-linear/svelte/native.svelte?raw';

import Dir from '@/examples/components/progress-linear/svelte/dir.svelte';
import DirRaw from '@/examples/components/progress-linear/svelte/dir.svelte?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Default client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="svelte" />
	</Fragment>
</Preview>

## Color

Provide background color values for the track and range to customize the colors.

<Preview client:load>
	<Fragment slot="preview">
		<Color client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ColorRaw} lang="svelte" />
	</Fragment>
</Preview>

## Height

<Preview client:load>
	<Fragment slot="preview">
		<Height client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={HeightRaw} lang="svelte" />
	</Fragment>
</Preview>

## Orientation

For vertical orieintation, a height must be defined on the Track component. The default is `h-[100px]`.

<Preview client:load>
	<Fragment slot="preview">
		<Orientation client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={OrientationRaw} lang="svelte" />
	</Fragment>
</Preview>

## Indeterminate

You must explicitely specify a `null` value to trigger indeterminate mode.

<Preview client:load>
	<Fragment slot="preview">
		<Indeterminate client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={IndeterminateRaw} lang="svelte" />
	</Fragment>
</Preview>

### Custom Animation

<Preview client:load>
	<Fragment slot="preview">
		<CustomAnimation client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={CustomAnimationRaw} lang="svelte" />
	</Fragment>
</Preview>

Consider using following variants to target the orientation and state.

- `data-[orientation=horizontal]:my-custom-animation` - target the horizontal orientation.
- `data-[orientation=vertical]:my-custom-animation` - target the vertical orientation.
- `data-[state=indeterminate]:my-custom-animation` - target the indeterminate state.

## Native Alternative

The native [`progress`](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/progress) element is available cross browser, but does not support indeterminate animations.

<Preview client:load>
	<Fragment slot="preview">
		<Native client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={NativeRaw} lang="svelte" />
	</Fragment>
</Preview>

## Direction

<Preview client:load>
	<Fragment slot="preview">
		<Dir client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DirRaw} lang="svelte" />
	</Fragment>
</Preview>

## API Reference

<ApiTable component="progress" />
