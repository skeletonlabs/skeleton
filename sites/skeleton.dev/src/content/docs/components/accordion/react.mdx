---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Default from '@examples/components/accordion/react/default';
import DefaultRaw from '@examples/components/accordion/react/default?raw';

import Controlled from '@examples/components/accordion/react/controlled';
import ControlledRaw from '@examples/components/accordion/react/controlled?raw';

import Collapsible from '@examples/components/accordion/react/collapsible';
import CollapsibleRaw from '@examples/components/accordion/react/collapsible?raw';

import Multiple from '@examples/components/accordion/react/multiple';
import MultipleRaw from '@examples/components/accordion/react/multiple?raw';

import Columns from '@examples/components/accordion/react/columns';
import ColumnsRaw from '@examples/components/accordion/react/columns?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Default client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="tsx" />
	</Fragment>
</Preview>

## Controlled

Use `value` and `onValueChange` to control the value of the Accordion.

<Preview client:load>
	<Fragment slot="preview">
		<Controlled client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ControlledRaw} lang="tsx" />
	</Fragment>
</Preview>

## Collapsible

By default you can't close open items. Adding `collapsible` changes this behavior.

<Preview client:load>
	<Fragment slot="preview">
		<Collapsible client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={CollapsibleRaw} lang="tsx" />
	</Fragment>
</Preview>

## Multiple

Adding `multiple` allows items to open independently.

<Preview client:load>
	<Fragment slot="preview">
		<Multiple client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={MultipleRaw} lang="tsx" />
	</Fragment>
</Preview>

## Grid Columns

Use the [grid-cols-\*](https://tailwindcss.com/docs/grid-template-columns) utility to adjust the layout of the Trigger component.

<Preview client:load>
	<Fragment slot="preview">
		<Columns client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ColumnsRaw} lang="tsx" />
	</Fragment>
</Preview>

## API Reference

<ApiTable />
