---
layout: '@/components/layouts/LayoutDoc.astro'
---

import DefaultRaw from '@/components/examples/components/accordion/react/default?raw';
import Default from '@/lib/components/examples/components/accordion/react/default';

import ControlledRaw from '@/components/examples/components/accordion/react/controlled?raw';
import Controlled from '@/lib/components/examples/components/accordion/react/controlled';

import CollapsibleRaw from '@/components/examples/components/accordion/react/collapsible?raw';
import Collapsible from '@/lib/components/examples/components/accordion/react/collapsible';

import MultipleRaw from '@/components/examples/components/accordion/react/multiple?raw';
import Multiple from '@/lib/components/examples/components/accordion/react/multiple';

import ColumnsRaw from '@/components/examples/components/accordion/react/columns?raw';
import Columns from '@/lib/components/examples/components/accordion/react/columns';

import DirRaw from '@/components/examples/components/accordion/react/dir?raw';
import Dir from '@/lib/components/examples/components/accordion/react/dir';

<Preview client:load>
	<Fragment slot="preview">
		<Default client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="tsx" />
	</Fragment>
</Preview>

## Controlled

Use `value` and `onValueChange` to control the value of the Accordion.

<Preview client:load>
	<Fragment slot="preview">
		<Controlled client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ControlledRaw} lang="tsx" />
	</Fragment>
</Preview>

## Collapsible

By default you can't close open items. Adding `collapsible` changes this behavior.

<Preview client:load>
	<Fragment slot="preview">
		<Collapsible client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={CollapsibleRaw} lang="tsx" />
	</Fragment>
</Preview>

## Multiple

Adding `multiple` allows items to open independently.

<Preview client:load>
	<Fragment slot="preview">
		<Multiple client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={MultipleRaw} lang="tsx" />
	</Fragment>
</Preview>

## Grid Columns

Use the [grid-cols-\*](https://tailwindcss.com/docs/grid-template-columns) utility to adjust the layout of the Trigger component.

<Preview client:load>
	<Fragment slot="preview">
		<Columns client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ColumnsRaw} lang="tsx" />
	</Fragment>
</Preview>

## Direction

<Preview client:load>
	<Fragment slot="preview">
		<Dir client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DirRaw} lang="tsx" />
	</Fragment>
</Preview>

## API Reference

<ApiTable />
