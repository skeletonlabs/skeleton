---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Example from '@examples/components/switch/Example.svelte';
import ExampleRaw from '@examples/components/switch/Example.svelte?raw';
import ExampleList from '@examples/components/switch/ExampleList.svelte';
import ExampleListRaw from '@examples/components/switch/ExampleList.svelte?raw';
import ExampleIcons from '@examples/components/switch/ExampleIcons.svelte';
import ExampleIconsRaw from '@examples/components/switch/ExampleIcons.svelte?raw';
import ExampleLightswitch from '@examples/components/switch/ExampleLightswitch.svelte';
import ExampleLightswitchRaw from '@examples/components/switch/ExampleLightswitch.svelte?raw';
import ExampleCompact from '@examples/components/switch/ExampleCompact.svelte';
import ExampleCompactRaw from '@examples/components/switch/ExampleCompact.svelte?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Example client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

## List

<Preview client:load>
	<Fragment slot="preview">
		<ExampleList client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleListRaw} lang="svelte" />
	</Fragment>
</Preview>

## Icons

Pass icons through the `inactiveChild` and `activeChild` snippets respectively.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleIcons client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleIconsRaw} lang="svelte" />
	</Fragment>
</Preview>

## Compact

Apply the `compact` prop to switch a minimal display.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleCompact client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleCompactRaw} lang="svelte" />
	</Fragment>
</Preview>

## Light Switch

This is a full fledge example using multiple features.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleLightswitch client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleLightswitchRaw} lang="svelte" />
	</Fragment>
</Preview>

## Anatomy

<Anatomy label="Switch" tag="label" isComponent>
	<Anatomy tag="input" />
	<Anatomy label="control" tag="span">
		<Anatomy label="thumb" tag="span">
			<Anatomy label="iconActiveBase|iconInactiveBase" tag="span" />
		</Anatomy>
	</Anatomy>
	<Anatomy label="label" tag="span" />
</Anatomy>

## API Reference

<ApiTable />
