---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Example from '@examples/components/avatars/Example.svelte';
import ExampleRaw from '@examples/components/avatars/Example.svelte?raw';
import ExampleFallback from '@examples/components/avatars/ExampleFallback.svelte';
import ExampleFallbackRaw from '@examples/components/avatars/ExampleFallback.svelte?raw';
import ExampleInitials from '@examples/components/avatars/ExampleInitials.svelte';
import ExampleInitialsRaw from '@examples/components/avatars/ExampleInitials.svelte?raw';

import ExampleFilter from '@examples/components/avatars/ExampleFilter.svelte';
import ExampleFilterRaw from '@examples/components/avatars/ExampleFilter.svelte?raw';
import ExampleStyle from '@examples/components/avatars/ExampleStyle.svelte';
import ExampleStyleRaw from '@examples/components/avatars/ExampleStyle.svelte?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Example client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

## Initials

Provide an `initials` array to define what part of the name is used to generate initials. Use index values to identify each desired name, and `-1` to represent the last possible name.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleInitials />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleInitialsRaw} lang="svelte" />
	</Fragment>
</Preview>

## Fallback

When `src` or `srcSet` are missing or fail to load, a fallback will be displayed using the default `children`. This can be used to display icons or implement a custom image using [@sveltejs/enhanced-img](https://kit.svelte.dev/docs/images#sveltejs-enhanced-img).

<Preview client:load>
	<Fragment slot="preview">
		<ExampleFallback />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleFallbackRaw} lang="svelte" />
	</Fragment>
</Preview>

## Filters

Avatars can implement [SVG Filters](/docs/guides/cookbook/svg-filters) using arbitrary Tailwind syntax.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleFilter client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleFilterRaw} lang="svelte" />
	</Fragment>
</Preview>

## Style Attribute

Use the `style` prop to implement custom styles on the parent `<figure>` element.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleStyle />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleStyleRaw} lang="svelte" />
	</Fragment>
</Preview>

## Anatomy

<Anatomy label="Avatar" tag="figure" isComponent>
	<Anatomy label="image" tag="img" />
	<Anatomy label="fallback" tag="span" />
</Anatomy>

## API Reference

<ApiTable />
