---
layout: '@/components/layouts/LayoutDoc.astro'
---

import DefaultRaw from '@/components/examples/components/file-upload/react/default?raw';
import Default from '@/lib/components/examples/components/file-upload/react/default';

import CustomContentRaw from '@/components/examples/components/file-upload/react/custom-content?raw';
import CustomContent from '@/lib/components/examples/components/file-upload/react/custom-content';

import DisabledRaw from '@/components/examples/components/file-upload/react/disabled?raw';
import Disabled from '@/lib/components/examples/components/file-upload/react/disabled';

import ButtonRaw from '@/components/examples/components/file-upload/react/button?raw';
import Button from '@/lib/components/examples/components/file-upload/react/button';

import ClearFilesRaw from '@/components/examples/components/file-upload/react/clear-files?raw';
import ClearFiles from '@/lib/components/examples/components/file-upload/react/clear-files';

import DirRaw from '@/components/examples/components/file-upload/react/dir?raw';
import Dir from '@/lib/components/examples/components/file-upload/react/dir';

<Preview client:load>
	<Fragment slot="preview">
		<Default client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="tsx" />
	</Fragment>
</Preview>

## Custom Content

Supply your own text and icons within the dropzone.

<Preview client:load>
	<Fragment slot="preview">
		<CustomContent client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={CustomContentRaw} lang="tsx" />
	</Fragment>
</Preview>

## Disabled

<Preview client:load>
	<Fragment slot="preview">
		<Disabled client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DisabledRaw} lang="tsx" />
	</Fragment>
</Preview>

## Button Only

<Preview client:load>
	<Fragment slot="preview">
		<Button client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ButtonRaw} lang="tsx" />
	</Fragment>
</Preview>

## Clear Files

{/* TODO: Link to Provider pattern explanation */}

Use the `Provider` pattern to access the `clearFiles` method.

<Preview client:load>
	<Fragment slot="preview">
		<ClearFiles client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ClearFilesRaw} lang="tsx" />
	</Fragment>
</Preview>

## Direction

<Preview client:load>
	<Fragment slot="preview">
		<Dir client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DirRaw} lang="tsx" />
	</Fragment>
</Preview>

## API Reference

<ApiTable />
