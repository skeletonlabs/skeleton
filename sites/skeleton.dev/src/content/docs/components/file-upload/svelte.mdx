---
layout: '@layouts/LayoutDoc.astro'
---

export const components = componentSet;

import Default from '@examples/components/file-upload/svelte/default.svelte';
import DefaultRaw from '@examples/components/file-upload/svelte/default.svelte?raw';

import ClearFiles from '@examples/components/file-upload/svelte/clear-files.svelte';
import ClearFilesRaw from '@examples/components/file-upload/svelte/clear-files.svelte?raw';

<Preview client:load>
	<Fragment slot="preview">
		<Default client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={DefaultRaw} lang="svelte" />
	</Fragment>
</Preview>

## Clear Files

{/* TODO: Link to Provider pattern explanation */}

Use the `Provider` pattern to access the `clearFiles` method.

<Preview client:load>
	<Fragment slot="preview">
		<ClearFiles client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ClearFilesRaw} lang="svelte" />
	</Fragment>
</Preview>

## API Reference

<ApiTable />
