---
layout: '@/components/layouts/LayoutDoc.astro'
title: Presets
description: Canned styles for your interface elements.
srcCore: '/utilities/presets.scss'
order: 20
---

import Example from '@/components/examples/design/presets/Example.svelte';
import ExampleRaw from '@/components/examples/design/presets/Example.svelte?raw';
import ExampleCustomRaw from '@/components/examples/design/presets/ExampleCustom.astro?raw';
import ExampleFilledRaw from '@/components/examples/design/presets/ExampleFilled.astro?raw';
import ExampleGlassRaw from '@/components/examples/design/presets/ExampleGlass.astro?raw';
import ExampleGradientRaw from '@/components/examples/design/presets/ExampleGradient.astro?raw';
import ExampleOutlinedRaw from '@/components/examples/design/presets/ExampleOutlined.astro?raw';
import ExampleTonalRaw from '@/components/examples/design/presets/ExampleTonal.astro?raw';
import ExampleCustom from '@/lib/components/examples/design/presets/ExampleCustom.astro';
import ExampleFilled from '@/lib/components/examples/design/presets/ExampleFilled.astro';
import ExampleGlass from '@/lib/components/examples/design/presets/ExampleGlass.astro';
import ExampleGradient from '@/lib/components/examples/design/presets/ExampleGradient.astro';
import ExampleOutlined from '@/lib/components/examples/design/presets/ExampleOutlined.astro';
import ExampleTonal from '@/lib/components/examples/design/presets/ExampleTonal.astro';

{

<p className="text-xl">
	Presets are pre-defined styles that allow you to quickly and easily style buttons, badges, cards, and more. Create by mixing Skeleton and
	Tailwind primitives.
</p>

}

<Preview client:load>
	<Fragment slot="preview">
		<Example client:visible />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleRaw} lang="svelte" />
	</Fragment>
</Preview>

1. **Filled** - a filled preset of the primary brand color.
2. **Tonal** - a tonal preset of the primary brand color.
3. **Outlined** - an outlined preset of the primary brand color.
4. **Glass** - a custom preset using background transparency and backdrop blur.
5. **Elevated** - mixes a filled preset with a shadow.
6. **Ghost** - has no style by default, but shows a tonal preset on hover.
7. **Ghost Icon** - has no style by default, but shows a branded tonal preset on hover.
8. **Gradient** - a custom preset generated using Tailwind gradient primitives.

## Skeleton Presets

Skeleton's provides the following opinionated set of styles, including accessible backgrounds and text colors.

### Filled

```
preset-filled-{color}-{lightModeShade}-{darkModeShade}
```

<Preview client:load>
	<Fragment slot="preview">
		<ExampleFilled />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleFilledRaw} lang="astro" />
	</Fragment>
</Preview>

### Tonal

```
preset-tonal-{color}
```

<Preview client:load>
	<Fragment slot="preview">
		<ExampleTonal />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleTonalRaw} lang="astro" />
	</Fragment>
</Preview>

### Outlined

```
preset-outlined-{color}-{shade}-{shade}
```

<Preview client:load>
	<Fragment slot="preview">
		<ExampleOutlined />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleOutlinedRaw} lang="astro" />
	</Fragment>
</Preview>

## Custom Presets

Consider these best practices when creating presets:

- Custom presets are only limited by your imagination.
- Use any combination of Skeleton or Tailwind-provided primitive to generate a preset.
- Apply presets to any relevant element, including: buttons, cards, inputs, and more.
- Use a set naming convention, such as `preset-{foo}` to keep things standardized.
- Implement all presets in using Tailwind's [@utility directive](https://tailwindcss.com/docs/functions-and-directives#utility-directive) in your global stylesheet.
- Abstrast presets to a stylesheet or NPM package for shared used between projects.

Please be aware the following presets are not included by Skeleton. Rather, these are examples of how you might utilize the Preset pattern.

### Input Presets

<Preview client:load>
	<Fragment slot="preview">
		<ExampleCustom />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleCustomRaw} lang="astro" />
	</Fragment>
</Preview>

### Gradient Presets

Tailwind provides a number of powerful [Gradient](https://tailwindcss.com/docs/gradient-color-stops) utility classes that can be used to generate presets.

<Preview client:load>
	<Fragment slot="preview">
		<ExampleGradient />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleGradientRaw} lang="astro" />
	</Fragment>
</Preview>

### Glass Presets

<Preview client:load>
	<Fragment slot="preview">
		<ExampleGlass />
	</Fragment>
	<Fragment slot="code">
		<Code code={ExampleGlassRaw} lang="astro" />
	</Fragment>
</Preview>
