---
import Layout from '@layouts/Layout.astro';

const colorNames = ['primary', 'secondary', 'tertiary', 'success', 'warning', 'error', 'surface'];
const colorShades = [50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950];
---

<Layout>
	<div class="space-y-10">
		<header><h1 class="h1">Colors</h1></header>
		<section class="grid grid-cols-11 gap-1">
			{
				colorNames.map((name) =>
					colorShades.map((shade) => (
						<div class="w-full p-2 flex justify-center items-center" style={`background: var(--color-${name}-${shade})`}>
							<span class="text-xs text-center" style={`color: var(--color-${name}-contrast-${shade})`}>
								<small class="block">{name}</small>
								<strong class="block">{shade}</strong>
							</span>
						</div>
					))
				)
			}
		</section>
	</div>
</Layout>
